# Real-Data Fixtures

Each fixture has:

- `*.fixture.json`: synthetic-but-realistic input trace derived from observed room conditions.
- `*.expected.json`: expected classifications, confidence floors, and failure messaging.

The fixture runner processes these traces through the same analysis engine used by the live app.
